<div class="container">
  <div class="card-form">
    <div class="card-form-infos">
      <%= render 'shared/tabs_new_post' %>
      <p>Here you can ask your neighbours something</p>
      <%= simple_form_for(@question) do |f| %>
      <%= f.input :title %>
      <%= f.input :content %>
      <%= f.submit :submit, class: "btn btn-info" %>
    <% end %>
    </div>
  </div>
</div>
